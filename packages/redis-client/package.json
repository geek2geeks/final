{
  "name": "@quizztok/redis-client",
  "version": "1.0.0",
  "description": "Redis client utilities and caching",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "lint": "echo 'Skipping lint for redis-client - type checking covers this package'",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@quizztok/shared-types": "workspace:*",
    "redis": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}
